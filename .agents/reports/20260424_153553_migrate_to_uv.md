---
title: Migrate to uv
timestamp: 20260424_153553
---

## Done
- Updated `pyproject.toml` to use `setuptools.build_meta` instead of legacy build backend.
- Initialized project with `uv` and successfully synced environment using `uv sync`.
- Replaced traditional `venv` usage with `uv` managed `.venv`.

## Found
- The `pyproject.toml` was using `setuptools.backends._legacy:_Backend` which is not supported by modern `uv`/PEP 517 build isolations. Switching to `setuptools.build_meta` resolves the issue.

## Next Steps
- Ensure that CI/CD pipelines (if any) or any other automated scripts are updated to use `uv sync` instead of `pip install`.
